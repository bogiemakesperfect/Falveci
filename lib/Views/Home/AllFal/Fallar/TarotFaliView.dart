import 'package:flutter/material.dart';

class TarotFaliView extends StatelessWidget {
  const TarotFaliView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}